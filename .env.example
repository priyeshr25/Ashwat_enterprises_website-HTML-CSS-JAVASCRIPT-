# Copy this file to .env and fill in your real values.
# .env is gitignored — never commit it or share it (or a screenshot of it).

# Get this from @BotFather on Telegram after running /newbot
# Paste ONLY the token — no quote marks around it.
TELEGRAM_BOT_TOKEN=8909710420:AAErdi7TSsZT7ZKqKs0c26wCAJGls6ZVxNY

# Message your bot once, then visit:
# https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates
# and copy the "chat":{"id": ...} number here — plain number, no quotes.
TELEGRAM_CHAT_ID=502928878

# Optional — defaults to 3000
PORT=3000
